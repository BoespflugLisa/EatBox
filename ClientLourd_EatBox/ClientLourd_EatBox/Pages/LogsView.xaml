<Page x:Class="ClientLourd_EatBox.Pages.LogsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ClientLourd_EatBox.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LogsView">

    <Grid>
        <DataGrid x:Name="LogsTab" IsReadOnly="True" CanUserAddRows="false" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date"  Binding="{Binding Date}"/>
                <DataGridTextColumn Header="Nom" Binding="{Binding Username}" />
                <DataGridTextColumn Header="Info" Binding="{Binding Message}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
